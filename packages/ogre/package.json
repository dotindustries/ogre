{
  "name": "@dotinc/ogre",
  "repository": {
    "type": "git",
    "url": "https://github.com/dotindustries/ogre.git"
  },
  "version": "0.0.0",
  "description": "Git-like repository for in-memory object versioning",
  "main": "lib/index.ts",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "nyc ava",
    "test.watch": "ava --watch",
    "coverage": "nyc report",
    "coverage:html": "nyc report --reporter=html",
    "coverage:coveralls": "mkdir -p coverage && npm run coverage && nyc report --reporter=text-lcov > ./coverage/lcov.info && pwd && stat ./coverage/lcov.info"
  },
  "ava": {
    "timeout": "60s",
    "files": [
      "./src/**/*.test.ts"
    ],
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "author": "János Veres @nadilas",
  "license": "MIT",
  "devDependencies": {
    "@ava/typescript": "^3.0.1",
    "@types/node": "^17.0.21",
    "@types/uuid": "^8.3.4",
    "ava": "^4.0.1",
    "coveralls": "^3.1.1",
    "nyc": "^15.1.0",
    "ts-node": "^10.7.0",
    "tslib": "^2.3.1",
    "turbo": "^1.1.5",
    "typescript": "^4.5.5",
    "uuid": "^8.3.2"
  }
}
