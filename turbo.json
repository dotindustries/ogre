{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "dev": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "lib/**"]
    },
    "test": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["coverage/**", ".nyc_output/**"]
    },
    "test.watch": {
      "cache": false
    },
    "coverage:html": {
      "dependsOn": ["^test"],
      "outputs": ["coverage/**"]
    }
  }
}
